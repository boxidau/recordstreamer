version: '2'
services:
  icecast:
    build: ./icecast
    ports:
      - "80:80"
    expose:
      - "80"
  darkice:
    privileged: true
    build: ./darkice
    depends_on:
      - icecast
  ddns:
    build: ./ddns
    network_mode: "host"